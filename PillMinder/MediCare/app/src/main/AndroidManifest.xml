<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.suyash.dell.medicare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".bmi1" />
        <activity android:name=".bmi2" />
        <activity android:name=".bmi3" />
        <activity android:name=".body" />
        <activity android:name=".InitialPage" />
        <activity android:name=".MyPref" />
        <activity android:name=".login" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".signup" />
        <activity android:name=".help"/>
        <activity android:name=".contactp"/>
        <activity android:name=".workingHours" />
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>